/**
 * Canonical build output directory name (relative to package root).
 * Must match `tsdown.config.ts` `outDir` (`package.json` `bin` → `dist/index.mjs`).
 */
export const CODEMAP_BUILD_OUTPUT_DIR = "dist";
