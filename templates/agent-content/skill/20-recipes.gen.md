<!--
  Generated by `assembleAgentContent("skill")` via `renderRecipesSection()`
  in src/application/agent-content.ts. This file exists to keep the
  section in lexical order — `*.gen.md` body is replaced at fetch time
  with the live recipe catalog. The fallback content below is used only
  when the renderer is unreachable; agents normally see the live table.
-->

## Recipe catalog

See the live catalog: `codemap query --recipes-json` or the MCP resource `codemap://recipes`.
