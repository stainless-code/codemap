---
"@stainless-code/codemap": patch
---

On `codemap agents init`, ensure `.codemap.*` is listed in `.gitignore` for Git projects (create `.gitignore` when missing, or append once). Repo `.gitignore` consolidated to `.codemap.*`.
