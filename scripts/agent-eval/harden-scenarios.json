{
  "version": 1,
  "description": "Harden-pr workflow eval scenarios (manual agent runs; not CI-automated yet).",
  "scenarios": [
    {
      "id": "harden-missing-test",
      "probeDir": "fixtures/harden-probes/missing-test",
      "skill": "harden-pr",
      "mode": "lite",
      "goldenFindings": "fixtures/harden-probes/missing-test/expected-findings.json",
      "acceptance": "fixtures/harden-probes/missing-test/acceptance.sh",
      "intentAnchor": "Add formatWidget — pure string formatter; do not change signature or return shape."
    }
  ]
}
