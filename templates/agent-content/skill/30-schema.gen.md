<!--
  Generated by `assembleAgentContent("skill")` via `renderSchemaSection()`
  in src/application/agent-content.ts. This file exists to keep the
  section in lexical order — `*.gen.md` body is replaced at fetch time
  with the current `createTables()` DDL of every table in `.codemap/index.db`. The fallback
  below is used only when the renderer is unreachable.
-->

## Schema reference

See the DDL: `codemap query "SELECT name, sql FROM sqlite_schema WHERE type='table' ORDER BY name"` (live against the open index), MCP resource `codemap://schema` (cached after first read per server process), or HTTP `GET /resources/{encoded-uri}` (encoded `codemap://schema`) against `codemap serve`.
