---
params: []
actions:
  - type: navigate-to-throws-jsdoc
    description: Symbols carrying structured @throws JSDoc tags.
---

# find-throws-jsdoc

Structured `@throws` tags from the `jsdoc_tags` substrate (Tier 5).

```bash
codemap query --recipe find-throws-jsdoc
```
