<!--
  Generated schema section — body replaced at fetch time with live DDL from
  the indexed database. Fallback below is used only when the renderer is
  unreachable.
-->

## Schema reference

See the DDL: `codemap query "SELECT name, sql FROM sqlite_schema WHERE type='table' ORDER BY name"` (live against the open index), MCP resource `codemap://schema` (cached after first read per server process), or HTTP `GET /resources/{encoded-uri}` (encoded `codemap://schema`) against `codemap serve`.
