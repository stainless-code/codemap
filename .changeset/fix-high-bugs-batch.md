---
"codemap": patch
---

Fix high-severity bugs (describe.each parent stack, git porcelain -z paths, CLI symlink entry, pr-comment TTY stdin) and medium bugs (changed-since -z paths, perf baseline RUNS guard, qualified typeof in type stringify).
