---
params: []
actions:
  - type: navigate-to-decorator-usage
    description: Decorator sites with optional resolved target_symbol_id.
---

# find-decorator-usage

Decorator sites linked to decorated symbols when resolvable (Tier 5).

```bash
codemap query --recipe find-decorator-usage
```
